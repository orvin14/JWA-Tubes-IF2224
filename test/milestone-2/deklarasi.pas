program Deklarasi;
variabel
  i, j, k : integer;
  rataRata : real;
  isValid : boolean;
  hurufAwal : char;
  nilai : larik [1 .. 100] dari integer;
  matriks : larik [1 .. 100] dari real;
  
mulai
  i := 1;
  hurufAwal := 'a';
  isValid := tidak (i > 100);
selesai.